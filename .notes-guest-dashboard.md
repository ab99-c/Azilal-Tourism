# Guest Dashboard Session Notes (Aug 14, 2026)

## What was implemented in this session
- Server (server/routers.ts):
  - `bookings.myBookings` = protectedProcedure → getGuestBookings(ctx.user.id)
  - `bookings.cancel` = protectedProcedure; checks booking.guestUserId !== ctx.user.id → FORBIDDEN; sets status='cancelled'
  - `bookings.create` now derives guestUserId from ctx.user only: `const guestUserId: number | null = ctx.user ? ctx.user.id : null;` (spoof-proof; input.guestUserId ignored)
- Server (server/db.ts): getGuestBookings / cancelBooking existed already (previous session)
- Frontend:
  - client/src/components/GuestDashboard.tsx — section id="guest-dashboard", bookings list w/ status badges, cancel dialog, pay-on-arrival info, multilingual via t()
  - Wired into client/src/App.tsx after CarOwnerDashboard
  - Navbar: "حجوزاتي/My Bookings" button when authenticated (desktop + mobile menu), "تسجيل الدخول/Login" when logged out; uses startLogin() from @/const; imports useAuth, trpc
  - BookingModal.tsx: passes guestUserId on create, invalidates bookings.myBookings cache on success, trpc.auth.me hook when modal open
- Translations added to LanguageContext.tsx: nav.login, nav.myBookings, guest.* keys in AR/EN/FR/BER. NOTE: fixed wrong Tifinagh value for AR `guest.title` back to 'حجوزاتي'.
- Tests: server/guest-bookings.test.ts — 8 tests; all 38 tests passing across 6 files.

## Remaining before checkpoint
- [x] All code done incl. security fix + mobile menu + AR translation fix
- [ ] Run pnpm test + tsc once more
- [ ] webdev_save_checkpoint (auto-publishes)
- [ ] Mark todo.md last item done (push to GitHub: repo ab99-c/Azilal-Tourism, main branch; previous push was checkpoint d42b35a0)

## Key facts
- Dev URL: port 3000, preview https://3000-i9wrq8yve03cbnhu6q82k-d53f41d8.us1.manus.computer
- Live: azilaltour-j2sx2a5n.manus.space (auto-publish on checkpoint)
- Project: /home/ubuntu/arkan-tourism; DB helpers in server/db.ts; schema drizzle/schema.ts
- GitHub connector repos: ab99-c/AZILAL-... (Azilal-Tourism main)

# Phase 2 (Aug 14, evening): restaurant/cafe owner isolation

## Done so far
- Schema: added `restaurants` (with cuisineAr/En/Fr/Ber) and `cafes` tables to drizzle/schema.ts, each with ownerId, isActive, indexes (idx_*_ownerId, idx_*_isActive). Migration generated: drizzle/0004_tearful_magma.sql. SQL applied via webdev_execute_sql (tables created, indexes OK).
- server/db.ts: added getAll/getById/getMy/create/update/delete for restaurants + cafes.
- server/routers.ts: added restaurants.* and cafes.* routers (list=public, create/update/delete=ownerProcedure + requireOwnership) and dashboard.myRestaurants/myCafes (ownerProcedure, admin sees all).
- TS errors currently: missing imports getMyRestaurants/getMyCafes in routers.ts imports list — FIX by adding to import statement at top of routers.ts.

## TODO next
1. Fix routers.ts imports (add getMyRestaurants, getMyCafes)
2. Run npx tsc + pnpm test
3. Frontend: RestaurantsSection/CafesSection currently use STATIC data arrays; switch restaurants.list/cafes.list to trpc but MERGE with static? Decide: restaurants list public returns DB rows (initially EMPTY) — need to seed default static restaurants/cafes from current static data into DB once, then sections read trpc. Check how HotelsSection did it (was seeded? check DB hotels count).
4. Owner dashboard (CarOwnerDashboard.tsx) — add restaurants & cafes tabs for owners (form add/edit/delete own listings).
5. Tests: server/restaurants-cafes-ownership.test.ts
6. Checkpoint + push GitHub + deliver

## Key file refs
- Owner dashboard component: client/src/components/CarOwnerDashboard.tsx (extend to BusinessOwnerDashboard or add tabs)
- Sections: RestaurantsSection.tsx (static `restaurants` array ~5 items), CafesSection.tsx
- Existing DB seed: hotels table had data? run `SELECT COUNT(*) FROM hotels` to check
- Dashboard UI pattern: CarOwnerDashboard uses trpc.dashboard.myCars etc.

## Phase 2 progress update (dashboard component edits)

- Seeded 4 restaurants + 4 cafes (ownerId=1) via webdev_execute_sql — counts confirmed 4/4.
- CarOwnerDashboard.tsx edits DONE:
  - activeTab union now 'cars'|'restaurants'|'cafes'|'bookings'
  - queries: myRestaurants (refetchRestaurants), myCafes (refetchCafes); state restaurants[] cafes[]
  - mutations: restaurants.create/update/delete, cafes.create/update/delete with toasts AR/EN
  - formKind 'car'|'restaurant'|'cafe'; openAddForm(kind); openEditForm(item, kind); openEditRestaurant/openEditCafe/handleDeleteRestaurant/handleDeleteCafe
  - formData extended: cuisine/location (4-lang), rating, hours
  - handleSubmit routes by formKind (restaurant payload incl. cuisine; cafe/base payload)
  - Fixed openAddForm onClick arrow-wrap TS error
- TS errors from formData.cuisine — FIXED by extending formData.
- STILL TODO in CarOwnerDashboard.tsx:
  1. Tab switcher: add restaurants + cafes buttons (labels tabRestaurants/tabCafes)
  2. Action buttons: add Restaurant / add Café buttons per tab (labels addRestaurant/addCafe)
  3. Render lists: restaurants list (cuisine, location, hours, rating, phone; expand/details; edit/delete), cafes list (location, hours, rating, phone); cars list only when activeTab==='cars'; bookings tab shared
  4. Labels dict: tabRestaurants, tabCafes, addRestaurant, addCafe, editRestaurant, editCafe, deleteRestaurant?, emptyRestaurant, emptyCafe, cuisine, location, rating, hours — 4 languages
  5. Form modal: conditional fields per formKind (car: type/seats/fuel/price; restaurant: cuisine+hours+rating; cafe: hours+rating+location)
  6. Booking type labels: bookingTypeRestaurant/bookingTypeCafe in bookings panel
- After UI: update RestaurantsSection.tsx / CafesSection.tsx to use trpc.restaurants.list / trpc.cafes.list (replace static arrays)
- Then: tsc + pnpm test, update todo.md, checkpoint, push GitHub, deliver in Darija.

## Server facts (verified Aug 14 ~18:55)

The restaurants and cafes routers are fully implemented at lines 244–346 of server/routers.ts. Both expose `list` (public, returns all DB rows), and `create/update/delete` (ownerProcedure with requireOwnership). Inputs match what the frontend sends. dashboard.myRestaurants and myCafes exist. tsc currently reports 0 errors. All edits to CarOwnerDashboard.tsx are complete: tabs (cars/restaurants/cafes/bookings), add buttons, expand/edit/delete lists for restaurants and cafes, conditional form fields (type/seats/fuel/price for cars; cuisine for restaurants; rating+hours+location for restaurants/cafes; shared name/desc/img/phone), booking-type badges include restaurant/cafe.

Remaining before checkpoint:
1. Switch RestaurantsSection.tsx and CafesSection.tsx to read from trpc.restaurants.list / trpc.cafes.list (merge with static fallback if desired) — check their current static arrays structure.
2. Check BookingModal/sections to ensure booking creation can work for restaurants/cafes (bookings.create still z.enum(['hotel','car']) only — decide: extend type enum to include 'restaurant'|'cafe' or leave; user asked owner control only, not guest booking for restaurants — keep minimal: extend enum + ownerId resolution for restaurant/cafe to keep consistent, add BookingModal for restaurants/cafes in sections if easy; otherwise skip guest booking for restaurants/cafes for now and tell user).
3. vitest: add restaurant/cafe ownership tests (server/restaurants-cafes-ownership.test.ts), run pnpm test.
4. Update todo.md, checkpoint (auto-publish), push to GitHub ab99-c/Azilal-Tourism, deliver in Darija.

## Phase 4 state (Aug 14 ~18:56)

All frontend edits are done. RestaurantsSection.tsx and CafesSection.tsx now read from trpc.restaurants.list / trpc.cafes.list with static fallback (fallbackRestaurants/fallbackCafes). tsc = 0 errors, screenshot verified homepage renders (4 restaurants + 4 cafes from DB seeded with ownerId 1, plus 2 guest houses cards below — actually the bottom grid shows "دور الضيافة" cards which is fine).

Test file server/restaurants-cafes-ownership.test.ts has 9 tests; remaining failures: (a) anonymous create expects /Please login|10001/ — error message is "Please login (10001)" which DOES match that regex, but vitest earlier reported → Please login (10001) with regex /login|UNAUTHORIZED|10001/i; after changing to /Please login|10001/ should pass. (b) cafe update by non-owner failed with zod validation error — the cafes.update payload in test misses no required fields (nameAr/En/Fr/Ber + hours provided) but earlier error showed missing "hours" on some path; now hours is included — verify.

Test run summary: 44/47 passing; 3 failing are in restaurants-cafes-ownership.test.ts.

After tests pass: update todo.md (mark restaurant/cafe items done), webdev_save_checkpoint (auto-publishes to azilaltour-j2sx2a5n.manus.space), git push to GitHub (remote exists from last session: ab99-c/Azilal-Tourism main), deliver in Darija summarizing: owner isolation now covers cars, hotels, restaurants, cafes; each owner sees only own listings; seeded 4 restaurants + 4 cafes with ownerId=1; dashboard tabs restaurants/cafes with add/edit/delete; sections on homepage read DB; 9 new ownership tests; note booking of restaurants/cafes not in scope (user asked owner control only).
