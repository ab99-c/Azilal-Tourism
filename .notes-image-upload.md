# Image upload feature — plan notes

## User request
Owners can upload images for their restaurants & cafes easily from the dashboard.

## Key facts learned
- `server/storage.ts`: `storagePut(relKey, data: Buffer|Uint8Array|string, contentType)` → `{ key, url }` where url = `/manus-storage/{key}` (auto-served via signed redirect).
- Max S3 object size via forge: keep uploads ≤ 5MB (validate client+server).
- restaurants & cafes tables have `image` text column already (nullable).
- Owner procedures exist: `ownerProcedure` in server/routers.ts (adds ctx.ownerId); `requireOwnership(ctx, item, itemKind)` gates update/delete; dashboard.myRestaurants/myCafes scoped.
- Cars/hotels also have image columns but user asked only restaurants & cafes.
- Test pattern: server/restaurants-cafes-ownership.test.ts uses `makeCtx(userId)` helper: `{ req: {headers:{}}, res: {clearCookie()}, user }`.
- Tests run with real DB; clean up test rows after runs (`DELETE FROM restaurants WHERE nameEn LIKE 'Test%'`).
- vitest: 47 tests passing in 7 files.
- Live domain: azilaltour-j2sx2a5n.manus.space; auto-publish on checkpoint; GitHub remote: `git push github main`.

## Plan
1. Server: add `restaurants.uploadImage` and `cafes.uploadImage` procedures (ownerProcedure; input: id + base64 data + fileName; validate mime ∈ jpg/png/webp, ≤4MB; storagePut(`restaurants/{userId}/...`); then updateRestaurant(id, { image: url }); requireOwnership check).
   - Alternative: base64 in tRPC input is fine for ≤4MB. Decode base64 → Buffer.
   - Add DB helper uploadRestaurantImage(id, url) simple.
2. Frontend: CarOwnerDashboard.tsx — in restaurant/cafe form add file input → FileReader → base64 → trpc.restaurants.uploadImage/useMutation; show preview; on success the item.image updates (invalidate queries).
3. RestaurantsSection/CafesSection already render `restaurant.image` — nothing needed if image url stored; verify fallback (existing static items keep images).
4. Tests: ownership gating for upload (non-owner → FORBIDDEN, anonymous → UNAUTHORIZED), success stores image.
5. Checkpoint + push github main.

## tRPC input note
Base64 string max length for zod: z.string().max(5_500_000).

## STATUS UPDATE (phase 3 - tests)
- Server done: uploadImage procedures in routers.ts (restaurants + cafes) with ownership check, ext allowlist (jpg/jpeg/png/webp), 4MB limit, storagePut to restaurants/{uid}/ and cafes/{uid}/, invalidates cache, returns {success, url}.
- db.ts: createRestaurant/createCafe now return { id: insertId }; router create procedures return { success, id }.
- Frontend done (CarOwnerDashboard.tsx):
  - uploadRestaurantImageMutation / uploadCafeImageMutation wired
  - handleImageFile; pendingImage state; create onSuccess uploads pending image with res.id (both restaurant and cafe)
  - Form: formKind !== 'car' shows image upload label + preview + dashed file input (jpeg/png/webp); cars keep URL input
  - Direct "رفع صورة" buttons on expanded restaurant + cafe list items (uploadExistingRestaurantImage/uploadExistingCafeImage)
  - Labels added: imgUpload, chooseFile, imgHint, uploadPhoto; Upload icon imported
- Tests: server/image-upload.test.ts (7 tests) ALL PASS.
- 1 flaky pre-existing test failed: restaurants-cafes-ownership.test.ts line ~47 — bug in test: `makeCtx(next() + 1)` increments nextId twice so dashboard caller gets different ownerId than creator. Fix: use stable ownerId variable.
- Vitest: 54 tests total; only that 1 fails.
- Next: fix flaky test, re-run tests, tsc, checkpoint (auto-publish), push to github, update todo.md.
- Live: https://azilaltour-j2sx2a5n.manus.space

## REMOVE IMAGE FEATURE (current request)
User: add "remove image" button so owners can delete their uploaded restaurant/cafe photos.
Done:
1. Server routers.ts: restaurants.removeImage + cafes.removeImage procedures (ownerProcedure, requireOwnership, updateRestaurant(id,{image:null}), invalidateCache, return {success:true}). tsc 0 errors.
2. CarOwnerDashboard.tsx: removeRestaurantImageMutation/removeCafeImageMutation (refetch + toast), handleRemoveRestaurantImage/handleRemoveCafeImage (window.confirm then mutate).
Remaining:
- Add "حذف الصورة" buttons in the UI: (a) expanded restaurant list item action row (after upload button, only when item.image exists), (b) same for cafes, (c) in the form when editing with an image (optional: clear formData.img + pendingImage).
- Add label removePhoto: { ar:'حذف الصورة', en:'Remove Photo', fr:'Supprimer', ber:'ⴽⴽⵙ ⵜⵡⵍⴰⴼⵜ' } and import Trash2 already imported.
- Tests: server/image-upload.test.ts — add removeImage ownership gating tests (anonymous → UNAUTHED, other owner → rejected, owner → clears image column to null, confirmed via getRestaurantById).
- vitest currently 54 passing; run pnpm test; npx tsc --noEmit; cleanup test rows (DELETE FROM restaurants/cafes WHERE nameEn LIKE 'Test%'); checkpoint (auto-publish); git push github main; mark todo items.
- Live: azilaltour-j2sx2a5n.manus.space ; GitHub ab99-c/Azilal-Tourism main
- Note: image column nullable text — set to null is fine.

## NEW REQUEST: post-login two-choice flow (Darija)
User wants: after someone logs in (email/OAuth), show TWO choices:
1. "I want to book" (بغيت نحجز) → scrolls to booking sections (#hotels/#cars/#restaurants/#cafes)
2. "I own a property" (عندي فندق/مقهى/سيارة) → open owner dashboard (section #owner-dashboard in CarOwnerDashboard)
All in same site.

### Navbar.tsx facts (read, current at this point):
- Login button: desktop line 136-147 `onClick={() => startLogin()}` with icon LogIn, label t('nav.login'); mobile line 211-220 same startLogin.
- Authenticated buttons: My Bookings scrolls to 'guest-dashboard' (line 122-134 desktop, 199-209 mobile).
- Imports: useLanguage, motion/AnimatePresence, scrollToSection from '@/lib/scroll', useAuth from '@/_core/hooks/useAuth', trpc, startLogin from '@/const'.
- Lang: useLanguage() returns {lang, setLang, t}; t() keys like 'nav.login', 'nav.myBookings', 'nav.home' etc.

### Plan:
- Add translation keys: choice.title, choice.guestTitle, choice.guestDesc, choice.ownerTitle, choice.ownerDesc (AR/EN/FR/BER) in LanguageContext.tsx (add to each lang block near nav keys).
- Create component client/src/components/LoginChoiceDialog.tsx: AnimatePresence dialog, two large cards:
  - Card 1 "بغيت نحجز" (CalendarCheck icon) → onClose + scrollToSection('hotels') or hero->categories; use 'hotels' + 'cars'.
  - Card 2 "عندي فندق/مقهى/سيارة" (Store icon) → onClose + scrollToSection('owner-dashboard').
- Navbar: after login completes, show dialog. Simplest: watch useAuth().isAuthenticated change — when user becomes authenticated AND a state flag 'showChoice' (persisted in sessionStorage key 'adrrar_choice_done' false), open dialog. Implementation: in Navbar, use trpc.auth.me.useQuery to know when user arrives; use useEffect over isAuthenticated: if authed and not sessionStorage.adrar_choice_shown → set show + mark sessionStorage. Also allow manual: user can click "لوحة التحكم" later; dialog must have "تخطي" (skip) + remember choice.
- Owner dashboard section id: check App.tsx/Home.tsx id — likely 'owner-dashboard'. Verify with grep before wiring.
- Then: vitest (no backend change → maybe skip; keep existing 60 passing), tsc, screenshot, checkpoint, push github main, mark todo, deliver.

## PROGRESS on post-login choice flow (update):
DONE so far:
1. CarOwnerDashboard.tsx section now has id="owner-dashboard" (was missing — for scrollToSection).
2. LanguageContext.tsx: added choice.* keys (title, guestTitle, guestDesc, ownerTitle, ownerDesc, skip) to ALL 4 lang blocks (ar ~line 79, en ~line 179, fr ~line 279, ber ~line 379).
3. Created client/src/components/LoginChoiceDialog.tsx — dialog with two cards (guest→scrollToSection('hotels'), owner→scrollToSection('owner-dashboard')), skip button, AnimatePresence, props {open, onClose}.
4. Navbar.tsx: imported LoginChoiceDialog; added showChoice state + prevAuth effect.

FIX NEEDED NOW:
- Navbar.tsx line 26 uses nonexistent `useAuthRef(isAuthenticated)` → replace with useRef-based pattern:
  `const prevAuth = useRef(isAuthenticated);` (import { useRef } from 'react' — Navbar already imports useState, useEffect; add useRef)
  then in the effect: `if (isAuthenticated && !prevAuth.current) { ... setShowChoice(true) }` followed by `prevAuth.current = isAuthenticated;` — NOTE: put `prevAuth.current = isAuthenticated` INSIDE the effect after the if, so it always syncs.
- Still TODO: render <LoginChoiceDialog open={showChoice} onClose={() => setShowChoice(false)} /> at end of Navbar JSX (before closing </nav>).
- Verify: tsc, screenshot, maybe run login flow test manually not possible; tests 60 pass unchanged (no backend change). Then checkpoint + push github main + update todo.md (mark 4 items: choice dialog, labels, wire-in, verify/checkpoint/push) + deliver.

Existing todo items at bottom of todo.md for this request (search "post-login two-choice flow").
